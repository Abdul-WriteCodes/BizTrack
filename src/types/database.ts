// Placeholder types so the app compiles before you generate the real ones.
// Once the migration is applied to your project, run:
//
//   supabase gen types typescript --project-id <your-project-ref> > src/types/database.ts
//
// and this file will be replaced with fully typed tables (users, products,
// sales, sale_items, expenses, restock_log, debts, debt_payments,
// suppliers, payments, user_activity, cashbook_entries).

// eslint-disable-next-line @typescript-eslint/no-explicit-any
export type Database = any;
